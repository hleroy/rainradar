#!/bin/bash

set -o errexit
set -o pipefail
set -o nounset

# The archiver is the ONLY place APScheduler runs. It waits for the DB
# (migrations are applied by the django web container's /start) before
# scheduling. Never scale this service beyond one replica.
exec python /app/manage.py run_archiver
